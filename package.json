{
  "name": "opnfn",
  "type": "module",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.16.0",
    "@miniflare/storage-redis": "^2.12.1",
    "miniflare": "^2.12.1",
    "typescript": "^4.8.2",
    "vitest": "^0.25.8",
    "vitest-environment-miniflare": "^2.11.0",
    "wrangler": "2.0.16"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev -l -e dev",
    "deploy": "wrangler publish",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "itty-router": "^2.6.1",
    "superjson": "^1.11.0"
  }
}
